%
\begin{restatable}[Weak Past (\Past*)]{definition}{DefWeakPast}\label{def:weak_past}
    %
    It may hold that \Sat*[\ValClocks]:[\Past]\ if $\exists\ValTime$ such that \Sat*[\ValClocks+\ValTime]:[\Const].
    %
\end{restatable}
%