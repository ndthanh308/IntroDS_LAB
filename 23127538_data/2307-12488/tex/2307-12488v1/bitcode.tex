\begin{lstlisting}[language=C,
    emph={setup_env,login_check_limits,check_limit_deny,check_user_access},
    caption={\textbf{An example of critical data.} \cc{aclp} determines whether the program accepts current user authentication or not. Attackers may corrupt this variable to bypass blacklist.},label=code:bit,captionpos=b]
   int setup_env( ... ) { ...
     aclp = login_check_limits(conf, 0, 1, &i); ...
     if (c == NULL && aclp == 0) { ...
       goto auth_failure;
     } ...
   }
   //login_check_limits ->check_limit ->check_limit_deny
   int check_limit_deny(config_rec *c, ...) { ...
     if (check_user_access(c->subset, "DenyUser")) {
       return 1;
     } ...
   }
\end{lstlisting}
