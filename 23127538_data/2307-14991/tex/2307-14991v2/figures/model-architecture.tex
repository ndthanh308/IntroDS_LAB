% Define block styles
\tikzstyle{cloud} = [draw, ellipse,fill=red!20, node distance=2.5cm,minimum height=2em]
\tikzstyle{io} = [trapezium, trapezium left angle=70, trapezium right angle=110,minimum height=2em,text centered, draw=black, fill=blue!30]
\tikzstyle{model} = [rectangle, draw, text width=10em, text badly centered, minimum height=2em, node distance=3cm, inner sep=0pt]
\tikzstyle{block} = [rectangle, draw, text width=10em, rounded corners, minimum height=2em]
  \tikzstyle{line} = [draw, -latex']
\begin{tikzpicture}[node distance = 1.5cm, auto]
    % Place nodes
    % \node [io] (init) {Initialize};
    % \node [cloud, above of=init] (Start) {Start};
    % \node [cloud, right of=init] (system) {system};
    \node [block] (javaEdits) {\texttt{\tiny<ReplaceOld> long <ReplaceNew> public long <ReplaceEnd>}};
    \node [block, below of=javaEdits] (csOld) {\tiny\texttt{internal virtual long LastModified() \dots}};
    \node [block, below of=csOld] (javaNew) {\tiny\texttt{internal virtual long LastModified() \dots} };
    \node[model, right of=csOld, node distance=5cm] (deltaModel) {\DeltaTool};
    \node [block, right of=deltaModel, node distance=5cm] (csEdits) {\texttt{\tiny<ReplaceOld> internal <ReplaceNew> public <ReplaceEnd>}};
    \node [block, below of=csEdits,] (csCode) {\texttt{\tiny public virtual long LastModified() ...}};
    % \node [decision, below of=evaluate] (decide) {Decision};
    % \node [cloud, below of=decide, node distance=3cm] (Stop) {Stop};
    % % Draw edges
    \path [line] (javaEdits) -- (deltaModel);
    \path [line] (csOld) -- (deltaModel);
    \path [line] (javaNew) -- (deltaModel);
    \path [line] (deltaModel) -- (csEdits);
    \path [line] (csEdits) -- (csCode);
    % \path [line] (decide) -- node {no}(Stop);
    % \path [line,dashed] (Start) -- (init);
    % \path [line,dashed] (system) -- (init);
    % \path [line,dashed] (system) |- (evaluate);
\end{tikzpicture}