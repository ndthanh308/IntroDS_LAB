\begin{table}[!t]
    \centering
    \setlength{\tabcolsep}{1.3mm}
    \caption{
    \textbf{Average feature mismatch} on image super-resolution networks (EDSR and RDN) and image classification network (ResNet-20). The metrics are measured on DIV2K validation set for SR networks and ImageNet validation set for the classification network.
    \cheeun{should change this to avg QE? for both weight and activation? or maybe change this into fig??? also, should use the same dataset (div2k val set) for inputs.}
    \cheeun{or change to swinir instead of rdn?}
    \cheeun{key: mismatch is more severe for SR than other tasks}
    } 
    % 똑같이 맞춰주기? to ImageNet validation set?
    \resizebox{0.9\linewidth}{!}{
            \begin{tabular}{llc ccc}
                \toprule
                Task & Model & Image-wise Variance & Channel-wise Variance \\
                \midrule
                Image super-resolution & EDSR ($\times4$) & 15.08 & 40.29\\
                Image super-resolution & RDN ($\times4$) & 6.40 & 58.14 \\
                Image classification & ResNet-20 & 0.04 & 0.09\\
                \bottomrule
            \end{tabular}
    }
    % \label{tab:sup-analyses-mismatch}
    \label{tab:intro-mismatch}
\end{table}