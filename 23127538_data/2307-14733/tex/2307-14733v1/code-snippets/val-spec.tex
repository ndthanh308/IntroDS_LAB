// Generated stub code
doReturn(2).when(someMock).getOffset();

// Developer-written stub code
doReturn(4).when(someMock).getOffset();

// In the class under test
int offset = someMock.getOffset() + 5;
if (3 <= offset && offset <= 10) {
    // ...
} else {
    // fail
}
