%% Compatibility, layout, and typography %%
\usepackage{lmodern}  % improves display of text
\usepackage[T1]{fontenc}  % allows LaTeX to output non-ASCII characters better; load after fonts if possible
\usepackage[english]{babel}  % sets language to American English
%\usepackage{csquotes}  % for formal quotations
\usepackage{fullpage}  % 1-inch margins all around
\usepackage{titling}  % can be used to create a title page
\usepackage{appendix}  % can be used to add ``Appendices'' title above first appendix
\usepackage{caption}  % fixes table--caption spacing in article class
\frenchspacing
\usepackage{microtype}  % improves typographic details (e.g., kerning)
\usepackage{fnpct}  % adjusts kerning of footnote marks (superscripts) before commas or periods


%% Bibliography %%
\usepackage[numbers, sort&compress]{natbib}
\bibliographystyle{plainnat}


%% Images %%
\usepackage{graphicx}  % allows inclusion of images in document
\graphicspath{{./images/}}  % location of images in file system

% Example:
%% Figure environment removed


%% Tables %%
\usepackage{booktabs}
%\usepackage{tabularx}


%% Math %%
\usepackage{mathtools, amssymb, amsthm}  % mathtools loads amsmath

\allowdisplaybreaks[1]  % allows page breaks within amsmath environments; optional value from 1-4 sets permissibility (4 being the highest)

% \mathchardef\mhyphen="2D  % hyphen for math mode (credit to Gernot Salzer)
\renewcommand*{\emptyset}{\varnothing}  % prettier empty set symbol
\usepackage{bm}  % use \bm for bold math symbols

% Use \phanrel to insert an invisible relation operator to align an equation split across two lines.
\newcommand*{\phanrel}{\mathrel{\phantom{=}}}
% Example:
% \begin{align*}
%     &\phanrel \text{a very long expression} \\
%     &= \text{another long expression}
% \end{align*}

\newcommand*{\Complex}{\mathbb{C}}
\newcommand*{\Reals}{\mathbb{R}}
\newcommand*{\Rationals}{\mathbb{Q}}
\newcommand*{\Integers}{\mathbb{Z}}
\newcommand*{\Naturals}{\mathbb{N}}

\DeclareMathOperator{\E}{\mathbb{E}}
\DeclareMathOperator*{\argmax}{arg\,max}
\DeclareMathOperator*{\argmin}{arg\,min}

% Use \set for set notation. Starred version \set* automatically resizes with contents.
\DeclarePairedDelimiterX\set[1]\lbrace\rbrace{\,\def\suchthat{\;\delimsize|\;}#1\,}
% Example:
% $\set{x \suchthat x > 0}$

\newcommand*{\greedy}[1]{{\small $\textsc{Greedy}_{#1}$}}


%% Graphs %%
\usepackage{tikz}
\usetikzlibrary{babel, positioning}


%% Algorithms %%
% \usepackage{algorithm}  % needed to float algorithms
% \usepackage{algpseudocode}  % loads algorithmicx package as well; use option ``noend'' to supress block ending lines
% \newcommand*{\algorithmautorefname}{Algorithm}  % for hyperref's autoref macro

% Use \LongState to replace \State when the contents of the line overflow. This preserves the indentation level of the first line.
% \newcommand*{\LongState}[2]{\State\parbox[t]{\dimexpr\linewidth-\dimexpr\algorithmicindent*#1}{#2\strut}}  % inspired by a macro designed by TeX.SX user Werner
% Example:
% \LongState{2}{very long line that needs to be indented 2 levels}


% Use \isitdefined to check if a command is defined.
\newcommand*{\isitdefined}[1]{\%\% command `#1' does \ifcsname#1\endcsname\else NOT \fi exist \%\%}
% Example:
% \isitdefined{potato}


%% Load last %%
\usepackage[colorlinks, allcolors=blue]{hyperref}  % enable hyperlinks; load before `ellipsis`
\hypersetup{
    pdfinfo={
        Title={Online Maximum Independent Set of Hyperrectangles},
        Author={Rishi Advani and Abolfazl Asudeh},  % TODO update for any double-blind submissions
        Keywords={maximum independent set; hyperrectangles; geometric intersection graphs; online algorithms; competitive ratio; adversary model}
    }
}
\usepackage{ellipsis}  % adjusts spacing of ellipses before punctuation