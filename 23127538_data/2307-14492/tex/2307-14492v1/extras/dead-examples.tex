%cause of death: language reconigised is regular, conisder fact that k is smaller than the number of states, so when looking for min(k, x), regardless of x, just need to check if x < k < |Q| so any number of counters can be tested in min(k, x, y, ..., z) and you can just save all their values in the states space.
% Figure environment removed

%cause of death: SEE ABOVE.
% Figure environment removed