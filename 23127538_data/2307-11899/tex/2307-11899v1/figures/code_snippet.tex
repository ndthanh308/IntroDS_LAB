\begin{minted}[
  frame=single,
  framesep=2mm,
  fontfamily=tt,
  fontsize=\footnotesize,
  bgcolor=black
]{python}
# Florida Task Configuration
APP_NAME = "python-app"
WORKFLOW_NAME = "python-workflow"

# Training Function
def trainer(model: bytearray, iteration_id):
    # Model File Loaded in Memory
    print("\tSize of the model file is", len(model))
    # FL Iteration/Epoch Identifier
    print("\tIteration id is", iteration_id)
    #####################################################################################
    #    TODO:                                                                          #
    #        - Train model                                                              #
    #        - Calculate the difference/gradient (trained model minus original model)   #
    #        - Return gradient as a list of floats                                      #
    #####################################################################################

    # Return Gradient Example:
    return [0.2, -0.12, -3.7, 2.1, -1.5]

# Florida Task Definitions
work_details = FederatedLearningClient.WorkflowDetails(
    app_name=APP_NAME,
    workflow_name=WORKFLOW_NAME,
    trainer=trainer,
    selector=None,
)

client = FederatedLearningClient.get_instance()
client.execute(
    endpoint=ENDPOINT,
    cert_path=SSL_CERT_PATH,
    # True to use HTTP1/REST or False to use HTTP2/GRPC
    isEndpointHttp1=False,
    workflows=[work_details],
    logger=ConsoleLogger(),
    event=None,
)
\end{minted}